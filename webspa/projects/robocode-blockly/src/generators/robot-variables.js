'use strict';

Blockly.Robocode['variable_energy'] = function(_) {
  return ['energy', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['field_height'] = function(_) {
  return ['fieldHeight', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['field_width'] = function(_) {
  return ['fieldWidth', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['variable_gun_bearing'] = function(_) {
  return ['gunBearing', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['variable_gun_heading'] = function(_) {
  return ['gunHeading', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['variable_gun_ready'] = function(_) {
  return ['gunReady', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['variable_heading'] = function(_) {
  return ['heading', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['variable_hit_by_bullet_angle'] = function(_) {
  return ['hitByBulletAngle', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['variable_hit_by_bullet_bearing'] = function(_) {
  return ['hitByBulletBearing', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['variable_hit_robot_angle'] = function(_) {
  return ['hitRobotAngle', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['variable_hit_robot_bearing'] = function(_) {
  return ['hitRobotBearing', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['variable_hit_wall_angle'] = function(_) {
  return ['hitWallAngle', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['variable_hit_wall_bearing'] = function(_) {
  return ['hitWallBearing', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['variable_others'] = function(_) {
  return ['others', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['variable_robot_x'] = function(_) {
  return ['robotX', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['variable_robot_y'] = function(_) {
  return ['robotY', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['variable_scanned_angle'] = function(_) {
  return ['scannedAngle', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['variable_scanned_bearing'] = function(_) {
  return ['scannedBearing', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['variable_scanned_distance'] = function(_) {
  return ['scannedDistance', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['variable_scanned_energy'] = function(_) {
  return ['scannedEnergy', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['variable_scanned_heading'] = function(_) {
  return ['scannedHeading', Blockly.Robocode.ORDER_ATOMIC];
};

Blockly.Robocode['variable_scanned_velocity'] = function(_) {
  return ['scannedVelocity', Blockly.Robocode.ORDER_ATOMIC];
};


